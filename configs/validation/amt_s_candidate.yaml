candidate_id: amt_s_candidate
dataset_version_id: stage1_default
test_manifest_path: dataset_versions/stage1_default/test_all.csv
output_dir: outputs/candidate_validation/amt_s/stage1_default
model:
  model_name: amt_s
  repo_name: AMT
  config_path: cfgs/AMT-S.yaml
  checkpoint_path: AMT/amt-s.pth
  device: cuda
  embt: 0.5
  scale_factor: 1.0
  divisor: 16
  strict_checkpoint: true
thresholds:
  min_psnr_mean: 25.0
  min_ssim_mean: 0.80
  max_lpips_mean: null
limit_samples: null
compute_lpips: true
lpips_device: cpu
lpips_net: alex
save_predictions: true
prediction_limit: 16
mlflow:
  enabled: true
  experiment_name: stage1-amt-validation
  run_name: amt_s_candidate_stage1_default
  tags:
    stage: stage1
    workflow: amt_candidate_validation
